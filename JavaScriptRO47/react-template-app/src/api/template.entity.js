const templateEntity = {
  // Tip: CRUD (CREATE / READ / UPDATE / DELETE)
  create: () => {},
  readById: () => {},
  readAll: () => {},
  updateById: () => {},
  deleteById: () => {},
};

export default templateEntity;
